# Workflows

> The core repeatable processes this department runs. Each workflow states its trigger, steps,
> inputs, outputs, and the quality gate it must clear.

## Workflow: <name>

**Trigger:** <what starts it>
**Owner:** <role/agent>

```
<step 1> → <step 2> → <step 3> → <output>
```

1. **<Step>** — <detail>. Input: <…>. Output: <…>.
2. **<Step>** — <detail>.

**Exit criteria:** <the gate this must pass — link to [quality-gates.md](quality-gates.md)>.
